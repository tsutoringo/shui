Implement the M1 backend in the shared workspace. You own only backend/data files: migrations/0001_m1.sql, src/server/auth.ts, src/server/api.ts, src/server/m1.ts (or similarly named backend helpers), src/env.d.ts, and worker/test configuration only if strictly required. Do not edit React route/component files, routeTree.gen.ts, or tests. Follow ROADMAP.md and this design: disable public Better Auth email sign-up with emailAndPassword.disableSignUp=true; configure requireEmailVerification, autoSignIn false, revokeSessionsOnPasswordReset true, email verification/reset callbacks using safe non-secret delivery (a development email sink is acceptable but never log tokens; expose a server-side test/dev email sink only if needed); add databaseHooks.session.create.before to fail closed for disabled/unmapped human principals. Add D1 migration with principals, human_principals, system_roles seeded root/user-admin/application-admin, system_role_grants, bootstrap_state singleton, invitations, audit_events, outbox_events, and rate_limit_buckets. Implement Elysia routes outside /api/auth with no-store and generic errors: GET /api/bootstrap or /api/setup status, POST /api/bootstrap/reserve and /api/bootstrap/complete (also aliases /api/setup as sensible), POST /api/invitations create (root-authenticated), GET public invite by token, POST invite claim/complete or a clear single accept endpoint, revoke, POST /api/users/:id/disable and /enable. Use timing-safe bootstrap token validation, SHA-256 base64url token hashes, normalized emails, conditional SQL updates / RETURNING for race-safe bootstrap/invite consumption, status markers for reconciliation. Controlled user creation must use await auth.$context internalAdapter.createUser + password.hash + linkAccount(createLocalAccountIssuer). Do not call public sign-up internally. Ensure exactly one root under concurrent bootstrap attempts, and reconcile existing Better Auth users if domain completion was interrupted. Ensure auth session reads are authoritative where relevant and disabling calls internalAdapter.deleteUserSessions. Use Elysia t schemas compatible with installed versions. Keep TypeScript strict and ASCII. Run typecheck/check if possible and report files/commands/results. Do not commit.