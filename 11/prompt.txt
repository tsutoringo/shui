Perform a final focused review of the current uncommitted M1 changes in /Users/tsutoringo/Documents/workspaces/private/project/mikan. Previous review findings were addressed by: hard-expiry now atomically disables the invitation's deterministic human principal and deletes its sessions instead of deleting accounts; expiry handling is idempotent for already-expired rows; active pending/claimed invitations are protected by a partial unique email index and conflict handling; controlled invitation acceptance can reset credentials for users with no active human mapping; OAuth custom claims now require email verification; consent accepts both redirect_uri and redirect/url response formats and renders requested claims. Inspect current code and report only concrete remaining high/medium correctness or security findings, with file/line references. Do not edit. Verification currently passes: pnpm check:write, pnpm test (15 tests), pnpm build, drizzle-kit check, and pnpm db:migrate.