Review current uncommitted M1 changes one last time for concrete security/correctness issues only. Recent fixes include: guarded human-principal creation tied to active claimed invitation; no forced expiry disable on already-processed invitations; claimed invitation revoke atomically disables principal and sessions; scheduled expiry reconciliation with expired_at tracking; resource-less api:read/client-credentials requests rejected at API onRequest; password reset for existing reconciled users is hashed; manual disable session deletion is inside the D1 batch; invitation delivery failures keep the pending invitation and mark its outbox failed while returning the link. Current verification passes check, 17 tests, build, drizzle check, and db:migrate. Do not edit files. Report only remaining actionable findings with severity and file/line refs, or no concrete findings, and distinguish residual testing gaps from bugs.