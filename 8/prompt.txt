Do one final code review of the current uncommitted M1 implementation. Check only concrete remaining correctness/security issues after these fixes: password reset uses context.password.hash before internalAdapter.updatePassword; expiry is tracked with invitations.expired_at, processed by scheduled expireStaleInvitations, atomically disables human/principal and deletes sessions, and emits audit/outbox; already-expired tokens do not re-disable rows with expired_at set; expired users can be reconciled through a new invitation; partial unique active-email invitation index exists; OAuth claims require email verification; consent supports redirect_uri and redirect/url and renders claims. Verification passes: check, 15 tests, build, drizzle check, db:migrate. Do not edit files. Return only actionable findings or say no concrete findings, plus residual gaps.